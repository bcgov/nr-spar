import React from 'react';

const a = <a href="/">go to orchard&apos;s management page</a>;
const textConfig = {
  tabTitle: 'Cone and pollen count',
  tabDescription: `Enter the cone and pollen count manually or
                   upload a spreadsheet file with the template for the cone and pollen count table.
                   Remember to keep your orchard updated, you can ${a}
                   to edit the listed parent trees in your orchard.
                   `
};

export default textConfig;
