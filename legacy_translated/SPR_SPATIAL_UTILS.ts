// Not working functions, just for error and reference for now

export const get_bec = (
  v_planting_site_point: object,
  v_bgc_zone_code: string,
  v_bgc_subzone_code: string,
  v_variant: string) => {}

export const get_spzb = (
  v_planting_site_point: object,
  v_vegetation_code: string
): string => {
  return 'hey';
}
