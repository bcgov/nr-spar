type AgencyTextPropsType = {
  useDefaultCheckbox: {
    name: string,
    labelText: string
  },
  agencyInput: {
    titleText: string,
    invalidText: string
  },
  locationCode: {
    name: string,
    labelText: string
  }
}

export default AgencyTextPropsType;
