# Jasper JARs must be visible to the DevTools restart classloader after the
# dependency is added or updated; otherwise JR* types fail during bean introspection.
restart.include.jasperreports=/jasperreports-.*\.jar
restart.include.jasperreports-fonts=/jasperreports-fonts-.*\.jar
