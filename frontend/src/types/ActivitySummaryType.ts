/**
 * Used for Activity Summary section in CONSEP.
 */
export type ActivitySummaryType = {
  activity: string,
  seedlotNumber: number,
  requestId: string,
  species: string,
  testResult: string
}
