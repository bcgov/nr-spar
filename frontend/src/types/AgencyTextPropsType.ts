type AgencyTextPropsType = {
  useDefaultCheckbox: {
    name: string,
    labelText: string
  },
  agencyInput: {
    name: string,
    labelText: string,
    invalidText: string
  },
  locationCode: {
    name: string,
    labelText: string
  }
}

export default AgencyTextPropsType;
